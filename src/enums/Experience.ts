/**
 * Possible phases when there is an animation happening
 */
export enum Phase {
  Ready,
  Playing,
  End,
  Pause,
}

/**
 * Possible games that can be on hover
 */
export enum GameOnHover {
  TLOU2_1,
  TLOU2_2,
  GOD_1,
  COD_1,
  COD_2,
}

/**
 * Sense music for about scene.
 */
export enum PlayMusic {
  Warning,
  Danger,
  Info,
  Success,
}
